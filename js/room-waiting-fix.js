/* GameVerse - authoritative online room bridge
 * Keeps the existing GameVerse UI. No bots. One Socket.IO connection for the room.
 * Chess state is owned by server.js; this file only handles room presence/waiting.
 */
(function () {
  'use strict';
  const BACKEND='https://masaoyunlari-backend.onrender.com';
  let socket=null,activeRoom=null,activeRoomId=null,started=false,patched=false;
  function state(){try{return typeof st!=='undefined'?st:null}catch(_){return null}}
  function name(){const s=state();if(s?.user?.name)return s.user.name;try{const u=JSON.parse(localStorage.getItem('gv-user')||localStorage.getItem('user')||'null');if(u?.name||u?.username)return u.name||u.username}catch(_){}return localStorage.getItem('gv-user-name')||'Oyuncu'}
  function userKey(){const s=state(),u=s?.user,stable=u&&(u.id||u.userId||u.username||u.email);if(stable)return'user:'+stable;try{const v=JSON.parse(localStorage.getItem('gv-user')||localStorage.getItem('user')||'null'),id=v&&(v.id||v.userId||v.username||v.email);if(id)return'user:'+id}catch(_){}let id=localStorage.getItem('gv-room-guest-key');if(!id){id=crypto?.randomUUID?crypto.randomUUID():'guest-'+Date.now()+'-'+Math.random().toString(36).slice(2);localStorage.setItem('gv-room-guest-key',id)}return'guest:'+id}
  function roomFromArg(r){if(r?.id)return r;const s=state();return window.__gvActiveRoom||s?.roomWaitingState?.room||null}
  function isChess(r){const s=state();return String(r?.gameId||r?.game||s?.curGame||window.__gvCurrentGame||'').toLowerCase()==='chess'||/satranç|chess/i.test(document.getElementById('grTitle')?.textContent||'')}
  function ensureCss(){if(document.getElementById('gv-room-wait-css'))return;const style=document.createElement('style');style.id='gv-room-wait-css';style.textContent='#gv-room-waiting-online{margin:18px auto;padding:18px 20px;max-width:720px;border-radius:16px;background:rgba(20,20,45,.96);border:1px solid rgba(255,255,255,.12);color:#eee;box-shadow:0 12px 40px rgba(0,0,0,.35);position:relative;z-index:1000}#gv-room-waiting-online h3{margin:0 0 8px;font-size:18px}#gv-room-waiting-online .count{font-weight:800;margin:12px 0;color:#a9a1ff}#gv-room-waiting-online .seat{display:flex;align-items:center;gap:10px;padding:10px 12px;margin:7px 0;border-radius:10px;background:rgba(255,255,255,.045)}#gv-room-waiting-online .dot{width:10px;height:10px;border-radius:50%;background:#666}.dot.on{background:#49e6ad}';document.head.appendChild(style)}
  function renderWaiting(r){if(started)return;ensureCss();let el=document.getElementById('gv-room-waiting-online'),page=document.getElementById('pg-room')||document.getElementById('room');if(!el){el=document.createElement('div');el.id='gv-room-waiting-online';if(page)page.prepend(el);else document.body.prepend(el)}const players=Array.isArray(r?.players)?r.players:[];el.innerHTML='<h3>♟️ Satranç Bekleme Salonu</h3><div>Gerçek oyuncularla eşleşme bekleniyor. Bot kullanılmıyor.</div>'+players.slice(0,2).map(p=>'<div class="seat"><span class="dot on"></span><strong>'+String(p.name||'Oyuncu')+'</strong><span>Bağlandı</span></div>').join('')+(players.length<2?'<div class="seat"><span class="dot"></span><span>Rakip bekleniyor...</span></div>':'')+'<div class="count">Oyuncular: '+Math.min(players.length,2)+' / 2</div>';el.style.display='block'}
  function loadChess(){if(!isChess(activeRoom)||!started)return;if(document.querySelector('script[data-gv-chess-online]'))return;const s=document.createElement('script');s.src='js/chess-online.js?v=20260817-11';s.async=false;s.dataset.gvChessOnline='1';s.onload=()=>window.dispatchEvent(new CustomEvent('gv:roomReady',{detail:{roomId:activeRoomId,gameId:'chess'}}));document.head.appendChild(s)}
  function join(){if(!socket?.connected||!activeRoomId)return;socket.emit('joinRoom',{roomId:activeRoomId,gameId:isChess(activeRoom)?'chess':(activeRoom?.gameId||state()?.curGame||'chess'),maxPlayers:2,durationMinutes:Number(activeRoom?.durationMinutes||10),userName:name(),userKey:userKey()})}
  function connect(){if(socket||!activeRoomId)return;const make=()=>{if(typeof window.io!=='function')return;socket=window.io(BACKEND,{transports:['websocket','polling'],reconnection:true,reconnectionAttempts:Infinity,reconnectionDelay:1000});window.__gvRoomSocket=socket;socket.on('connect',join);socket.on('roomUpdated',updated=>{if(!updated||String(updated.id)!==String(activeRoomId))return;activeRoom=updated;window.__gvActiveRoom=updated;if(updated.players.length>=2){started=true;const e=document.getElementById('gv-room-waiting-online');if(e)e.style.display='none';loadChess()}else renderWaiting(updated)});socket.on('gameStarted',p=>{if(!p||String(p.roomId)!==String(activeRoomId))return;started=true;const e=document.getElementById('gv-room-waiting-online');if(e)e.style.display='none';loadChess()});socket.on('playerLeft',()=>{if(activeRoom){started=false;renderWaiting(activeRoom)}})};if(window.io)make();else{const s=document.createElement('script');s.src=BACKEND+'/socket.io/socket.io.js';s.onload=make;s.onerror=()=>console.error('[RoomFix] Socket.IO yüklenemedi.');document.head.appendChild(s)}}
  function disableGenericChessRealtime(){if(window.GVGames&&typeof window.GVGames.initRealtime==='function'&&!window.GVGames.__chessRealtimeDisabled){const original=window.GVGames.initRealtime;window.GVGames.initRealtime=function(){if(this.currentGame==='chess'){console.log('[RoomFix] Generic chess realtime devre dışı.');return}return original.apply(this,arguments)};window.GVGames.__chessRealtimeDisabled=true}}
  function activate(r){activeRoom=roomFromArg(r);if(!activeRoom?.id)return;activeRoomId=String(activeRoom.id);window.__gvActiveRoom=activeRoom;window.__gvActiveRoomId=activeRoomId;started=false;disableGenericChessRealtime();renderWaiting(activeRoom);connect()}
  function patchLegacy(){disableGenericChessRealtime();if(patched||typeof window.startRoomWaitingProcess!=='function')return;const legacy=window.startRoomWaitingProcess;window.startRoomWaitingProcess=function(r){activate(r);return null};window.__gvOriginalRoomWaitingProcess=legacy;patched=true}
  function boot(){patchLegacy();const r=roomFromArg();if(r?.id)activate(r);setTimeout(patchLegacy,100);setTimeout(patchLegacy,500);setTimeout(patchLegacy,1500);setInterval(disableGenericChessRealtime,500)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
