'use strict';const assert=require('assert');const b=require('../bilardo-engine');let s=b.init();assert.strictEqual(s.balls.length,16);assert.strictEqual(b.shoot(s,1,0,0.5).reason,'not_your_turn');assert.strictEqual(b.shoot(s,0,0,0.5).ok,true);assert.strictEqual(s.shots,1);assert.strictEqual(s.turn,1);assert.strictEqual(b.shoot(s,1,0,1.5).reason,'bad_shot');console.log('OK bilardo-engine');
