'use strict';const assert=require('assert');const r=require('../reversi-engine');let s=r.init();assert.strictEqual(s.board.flat().filter(Boolean).length,4);assert.strictEqual(r.legalMoves(s).length,4);assert.strictEqual(r.play(s,0,2,3).ok,true);assert.strictEqual(s.board[3][3],'b');assert.strictEqual(s.turn,'w');assert.strictEqual(r.play(s,0,2,2).reason,'not_your_turn');console.log('OK reversi-engine');
